import 'package:flutter/material.dart';

class EntryPinScreen extends StatelessWidget {
  const EntryPinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Entry pin")],
      ),
    );
  }
}

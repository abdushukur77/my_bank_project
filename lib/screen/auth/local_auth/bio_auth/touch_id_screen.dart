import 'package:flutter/material.dart';

class TouchIdScreen extends StatelessWidget {
  const TouchIdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Touch ID")],
      ),
    );
  }
}
